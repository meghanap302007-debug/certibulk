{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome (http://localhost:5500)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start static server"
    }
  ]
}
